# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Authentication
*       /auth       							module:securesocial
*       /                                       module:litmus

# mockups
GET     /mockups/{mockup}                       Mockups.showMockup

# Home page
GET     /                                       Application.index
GET		/dashboard								Dashboard.dashboard
GET		/buySupplies							Supplies.index


GET     /old/horse                                  Horses.blank
GET     /old/horses                                 Horses.list
GET		/old/horses/buy/{id}						Horses.buy

GET		/old/races									Races.list
GET		/old/races/new								Races.newRace
POST	/old/races/new								Races.create
GET		/old/races/{<[0-9]+>id}						Races.detail
GET		/old/races/enter							Races.enter

# Jobs page for development
GET		/@jobs									Jobs.index

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

# Catch all
*       /{controller}/{action}                  {controller}.{action}

